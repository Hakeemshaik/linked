{
  "name": "linkup",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Friends planning PWA: free days, plans, chat, AI planner, push notifications, video calls.",
  "scripts": {
    "postinstall": "npm --prefix web install --no-audit --no-fund && npm --prefix server install --no-audit --no-fund",
    "setup": "npm install && npm run build",
    "build": "npm --prefix web run build",
    "start": "node server/src/index.js",
    "gate": "node scripts/ollama-gate.js",
    "dev:server": "node --watch server/src/index.js",
    "dev:web": "npm --prefix web run dev",
    "test:e2e": "node tests/two-people.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "gifenc": "^1.0.3",
    "playwright": "^1.56.0"
  }
}
